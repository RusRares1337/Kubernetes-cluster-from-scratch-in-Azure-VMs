apiVersion: v1
kind: Service
metadata:
  labels:
    app: sampleapp
  name: sampleapp
  namespace: dev-cluster
spec:
  ports:
  - name: helloworld
    nodePort: 30423
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: sampleapp
  sessionAffinity: None
  type: NodePort