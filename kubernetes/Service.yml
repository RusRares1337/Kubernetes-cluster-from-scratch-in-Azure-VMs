apiVersion: v1
kind: Service
metadata:
  name: myservice
  namespace: dev-cluster
spec:
  selector:
  	app: sampleapp
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30036
    protocol: TCP